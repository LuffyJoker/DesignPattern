状态模式：
    1、平时我们写代码，会由于各种业务逻辑，存在大量的 if else 语句。而状态模式，就是为了避免编写大量的 if else 语句。
    2、状态模式，是根据当前对象所处的不同状态，做出不同的行为

示例说明：
    1、Tv 存在开机、关机两种状态
    2、在开机时，设置当前 Tv 的状态为 PowerONState（ implements TvState）,之后，操作 Tv 时，直接就调用该状态的下的方法即可。
